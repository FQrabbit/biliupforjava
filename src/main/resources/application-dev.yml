server:
  port: 80
  forward-headers-strategy: native
  undertow:
    threads:
      worker: 16
      io: 4
spring:
  servlet:
    multipart:
      max-file-size: 512MB
      max-request-size: 600MB
  datasource:
    username: bup
    password: bup
    url: jdbc:mysql://mysql:3306/bup?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8&autoReconnect=true&rewriteBatchedStatements=true
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          #为spring data jpa saveAll方法提供批量插入操作 此处可以随时更改大小 建议500哦
          batch_size: 1000
          batch_versioned_data: true
          order_inserts: true
  thymeleaf:
    cache: false
  task:
    scheduling:
      pool:
        size: 10
#  h2:
#    console:
#      enabled: true

record:
  userName:
  password:
  work-path: /bilirecord
  wx-push-token: AT_oboGsjlaGphljfVS9TtC7JfuBB0xPJrn

bili:
  app-key: 4409e2ce8ffd12b8
  app-secret: 59b43e04ad6965f34319062b478f83dd

logging:
  level:
    org.springframework.web.servlet.DispatcherServlet: WARN
    top.sshh.bililiverecoder: DEBUG
  file:
    path: ${record.work-path}/log/
  logback:
    rollingpolicy:
      max-file-size: 50MB
      max-history: 14
      total-size-cap: 512MB
      clean-history-on-start: true

